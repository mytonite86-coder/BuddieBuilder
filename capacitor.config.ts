import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mytonite86.buddiebuilder',
  appName: 'BuddieBuilder',
  webDir: 'dist'
};

export default config;
